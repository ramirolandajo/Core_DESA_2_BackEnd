package ar.edu.uade.core.service;

import org.junit.jupiter.api.Test;
import org.springframework.test.util.ReflectionTestUtils;

import static org.junit.jupiter.api.Assertions.*;

class KafkaStartupVerifierExtraTest {

//    @Test
//    void verifyConnectivityOnStartup_ThrowsAfterExhaustingAttempts() {
//        KafkaStartupVerifier verifier = new KafkaStartupVerifier();
//        ReflectionTestUtils.setField(verifier, "bootstrapServers", "localhost:65531"); // puerto inválido
//        ReflectionTestUtils.setField(verifier, "maxAttempts", 1);
//        ReflectionTestUtils.setField(verifier, "backoffMs", 1L);
//        ReflectionTestUtils.setField(verifier, "requestTimeoutMs", 50L);
//
//        assertThrows(IllegalStateException.class, verifier::verifyConnectivityOnStartup);
//    }
}

